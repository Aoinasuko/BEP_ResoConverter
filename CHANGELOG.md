# 変更履歴

## [v0.3.4](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.3.4) — 2026-09-12

- 出力設定プリセットの新規保存・上書き保存・復元を追加。表情の左右条件・クリップ・時刻・表示名・行順、瞬き、サイズ、影の濃さなどをUnityアセットとして保持。
- 瞬き用メッシュは対象ルートからの相対階層で記録し、同じ構造の別の配置にも復元。欠落参照は通知し、曖昧な参照は現在の設定を変更せず停止。
- 復元はUndoに対応。Resonite本体の場所は現在のPCの設定を維持し、シーン参照や成果物のパスはプリセットへ保存しない。
- 初期保存先を `Assets/BEPFairyTech/ResoConverterPresets` とし、ツール本体の配布対象から分離。任意のVRChat SDK・Modular Avatar・lilToonへの依存は追加しない。

## [v0.3.3](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.3.3) — 2026-09-12

- PCでジャンプ・ダッシュなどに伴う指の姿勢変化がハンドサイン表情へ反映される問題を修正。PCではShift＋数字キーを操作している間だけハンドサインを使用。
- 左Shift＋数字1～8で左手、右Shift＋数字1～8で右手を切り替え、キーを離すと解除。1から順にIdle／Fist／Open／Point／Victory／RockNRoll／HandGun／ThumbsUp。
- VRでは従来のTouch・指追跡による表情切り替えを維持。PC操作はVRコントローラーのチェックをOFFにしても使用可能。メニューによる表情固定を優先。
- PCキー操作に非対応の旧変換エンジンを検出する互換性チェックを追加。修正の反映には元のUnityシーンから再変換が必要。

## [v0.3.2](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.3.2) — 2026-09-12

- PhysBoneのAngle／Hinge／Polarを表示ボーンへ適用。角度と制限軸のXYZ回転カーブをボーンごとに読み取り、VRCと同じ角度カーブの位置・回転順で変換。
- `ignoreOtherPhysBones` を反映し、別PhysBoneのRoot以下を重複して処理する問題を修正。分岐のFirst／Average／Ignoreも伝達。
- Resonite側の末端自動延長を停止。子ボーンも末端オフセットもない単一ノードのチェーンには、余分な揺れを発生させない。
- 制限付近では表示ボーンと衝突・つかみの計算位置がずれる可能性を警告。非一様・負スケールでのワールド空間の角度も近似であり、物理挙動の完全再現は行わない。
- 変換対象ルートそのものを角度制限付きPhysBoneのRootにした構成は、理由を表示して停止。配下の子ボーンをRootに指定する必要がある。
- 旧変換エンジンが角度制限を黙って無視することを防ぐ互換性チェックを追加。修正を反映するには元のUnityシーンから再変換が必要。取り込み先Resoniteへの追加MODは不要。
- Unity EditModeテストは任意パッケージありで74件成功、なしで54件成功・20件スキップ。強い外力を加えたサンプルの角度上限と、表示メッシュへの反映も検証。

## [v0.3.1](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.3.1) — 2026-09-11

- VRC Eye Lookの方向別ボーン回転を保持。一律10度の視線制御で、狭い可動範囲のアバターの目玉が逸脱する問題を修正。頭部の移動・回転にも継続して追従。
- 別BlendShapeによる目閉じ・ウィンクと通常瞬きが重なって顔が破綻する問題を修正。同じ頂点の変形を検出し、競合する表情の間だけ通常瞬きを抑制。
- 選択瞬きShapeの初期値0・表情キー0を通常瞬きへ戻す扱いに変更。
- トゥーン影の濃さを調整するスライダーを追加し、初期値を0.5に軽減。VRChat Mobile Toon Standardのトゥーン材質変換に対応。
- VRChat Mobile MultiplyをUnlitの乗算材質として出力し、透明度を白との補間にベイク。PBRによる不要な暗化を修正。
- 修正を反映するには元のUnityシーンから再変換が必要。

## [v0.3.0](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.3.0) — 2026-09-11

### 追加

- ハンドサイン表情の下に「コントローラー操作で手の形も切り替える」を追加。初期値はON。親項目「両手の形に合わせて表情を変える」の初期値は引き続きOFF。
- SteamVRのOculus/Meta Touchのトリガー接触・押し込み、グリップ、親指の接触からVRChat風の8種のハンドサインを判定し、表情条件へ使用。
- 有効時は手指の見た目も標準的な8種のポーズへ切り替え。アイテムやツールを保持する手は元の握り方を優先し、メニューによる表情固定は顔だけに適用。
- 新チェックをOFFにした場合や、Viveなど他のコントローラーでは、元の手指制御と指姿勢からの表情判定を使用。

### 修正

- v0.2.0で、標準の指追跡入力が有効でも位置情報が0になる場合に未追跡と誤判定し、ハンドサイン表情が反応しない問題へ対応。位置情報だけで判定せず、追跡状態を考慮する方式へ変更。

### 対応範囲・更新時の案内

- Viveのトラックパッド操作を8種のハンドサインへ割り当てる機能は未対応。現在の指姿勢からの近似判定を使用。
- Touchの対応はVRChat風の操作であり、VRChatのSteamVRバインドやカスタム操作設定そのものを取り込む機能ではない。親指はスティックやA/B/X/Yボタンへの接触・押下から判定し、ThumbRestへの接触は使わない。
- 新機能と修正を旧出力へ反映するには、元のUnityシーンからv0.3.0で再変換が必要。
- AnimationClipから指定時刻のBlendShape静止値を移す範囲、未指定Shapeの瞬き・口パク、メニュー優先の基本仕様は継続。
- 新しい変換結果に対し、標準の回転ストリーム、Touch入力25ケース、実際の指ボーンへの反映180箇所、掴み・解除とメニュー優先をエンジン内で検証。設定OFF・メニューのみ・アイテム出力も確認。VR機器での実操作と別PC間の表示は未確認。[検証記録](VERIFICATION.md)を参照。

## [v0.2.0](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.2.0) — 2026-09-11

### 追加

- アバター出力に「両手の形に合わせて表情を変える」「メニューから表情を変えられるようにする」を追加。個別に有効化でき、初期値は両方OFF。
- ハンドサインは左右それぞれVRChatで使われる8種と `Any` を指定。両手の条件に合う行を使い、複数一致時は上の行を優先。
- 表情ごとにAnimationClipとサンプル時刻を指定し、その時刻のBlendShapeの静止値を移植。時刻の既定値は0秒。
- メニューは表示名とクリップを複数登録可能。有効な項目が1件以上あれば「初期状態」を自動追加し、ハンドサインと併用する場合は「ハンドサインに戻す」も追加。
- メニューから選んだ表情はハンドサインより優先して固定。表情が指定していないShapeの瞬き・口パクを維持し、自動動作へ戻す際は現在の手形を反映。
- Modular Avatar前処理前のクリップ参照先を一時コピー上で保持し、処理後の対象へ対応付け。対象BlendShapeがないクリップや未解決の参照は変換を停止して通知。

### 対応範囲・更新時の案内

- 対応はBlendShapeの静止値のみ。Transform、材質、有効・無効などのキーは警告して対象外とし、アニメーションの再生や既存Expression Menu・Animatorロジックの自動移植は行わない。
- 手形は実際の指の姿勢から近似判定する。Resoniteの `FingerPosePreset` 内部列挙値をそのまま使うものではなく、機器や操作による認識差が生じる場合がある。
- 3Dモデル・アイテム出力では両表情設定を無効化し、表情操作用の仕組みを付けない。
- 既存の出力へ表情機能を追加するには、v0.2.0で設定して再変換が必要。v0.1.1以降の旧出力はそのまま使用可能。
- 完了した検証と実機での未確認項目は[検証記録](VERIFICATION.md)を参照。

### 検証

- Resoniteエンジンで最終パッケージを読み戻し、表情の81項目を検証。左右8種ずつの64組、行の優先順、メニュー固定・解除、疎な表情の初期値・自動値への復帰を確認。
- 標準の瞬きValueDriverに3種類の入力値を与え、中間フィールドを経由して元のBlendShapeへ届くことを確認。
- エンジンの装着・解除処理を実行し、装着ユーザーの左右指データ、表情メニューの登録・解除、2体のアバター間で表情選択が干渉しないことを確認。
- VR機器での手形認識・メニュー操作・実際の会話中の見た目は未確認。エンジン上の検証とは区別する。

## [v0.1.2](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.1.2) — 2026-09-11

### 変更

- クリップボードへのコピーを、ファイル一覧と文字列の併記からUnicode文字列のみへ変更。文字列を読み取る貼り付け経路への対応を目的とした変更。
- ボタン表示を「成果物を文字列としてコピー」へ変更。
- コピーする内容は `.resonitepackage` の絶対ファイルパスそのもの。パッケージ本体の文字列化・Base64化・URIへの変換は行わない。日本語・空白・絵文字を含むWindowsファイル名に対応。

### 更新時の案内

- v0.1.1で作成した出力ファイルは再変換不要。更新後に文字列としてコピーし直すことで利用できる。
- 同じPCのResoniteから取り込み、貼り付け完了までコピー元ファイルを移動・削除しないこと。文字列のみで別のPCへモデルを転送する機能ではない。
- 初期v0.1のアイテムを掴めない問題は、引き続き元のUnityシーンからの再変換が必要。コピーし直すだけでは古いファイルの当たり判定は修正されない。

### 検証範囲

- Unity MCPからのコピーと、別プロセスのWPFによる読み戻しで、文字列のみ・ファイル一覧なし・絶対パス一致を確認。
- 一時的なResoniteエンジン環境で、文字列だけのクリップボードから実際のパッケージインポーターへ到達し、モデルを読み込めることを確認。日本語・空白を含むパスでも成功。
- Resonite標準Dashの `Paste content from clipboard` ボタンによる実機動作は未確認。反応しない場合はキーボードの `Ctrl+V`、ドラッグ＆ドロップ、またはVR時のDashのFilesタブを使用。
- 詳細な検証結果は[検証記録](VERIFICATION.md)を参照。

## [v0.1.1](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.1.1) — 2026-09-11

### 修正

- 3Dモデル・アイテム出力で、当たり判定が空になり掴めない場合がある問題を修正。メッシュの読込完了を待って有効な頂点位置から範囲を計算し、BoxColliderの全軸に最低限の厚みを設定するように変更。
- 旧版で生成された不正なサイズのコライダーと、厚みのない平面モデルを回帰検証の対象に追加。

### 検証の追加

- コンポーネントの有無・件数の確認に加え、Resoniteエンジン上の6方向のレイ判定とGrab処理を検証。
- 問題が報告された2メッシュのパッケージを再現入力とし、修正後は保存制限ON/OFFの両方でレイが当たり、レーザー選択・手の接触に対応するGrab処理が成功することを確認。
- 詳細な範囲と実機での未確認項目は[検証記録](VERIFICATION.md)を参照。

### 取り込み案内の訂正・既知の問題

- v0.1で案内していた、Resonite標準Dashの `Paste content from clipboard` ボタンからの取り込みは、反応しない報告があり未解消。ボタンで取り込めるという案内を訂正。
- 代わりにキーボードの `Ctrl+V`、ファイルのドラッグ＆ドロップ、またはVR時のDashのFilesタブから取り込む手順を案内。
- 旧版で出力したアイテムは自動修正されないため、元のUnityシーンから再変換が必要。

## [v0.1](https://github.com/Aoinasuko/BEP_ResoConverter/releases/tag/v0.1) — 2026-09-11

初回リリース。

### 追加

- `Assets/BEPFairyTech/ResoConverter` へインストールするUnity Editorツール。
- `BEP Fairy Tech → ResoConverter` メニューからの操作。
- シーン上のアバター／3DモデルをResoniteネイティブの `.resonitepackage` へ出力。
- 保存制限を初期値ONとし、チェックで切り替える設定。
- アバターの瞬き用BlendShapeの選択、VRC Viseme・Jaw Flapの変換。
- アバターの標準サイズと元サイズの選択。
- 現在のポーズ・表情を固定した静的アイテム出力。
- PhysBoneの基本設定・半径カーブ・対応コライダーの近似変換と、元設定の記録。
- Modular AvatarのBone Proxy・Merge Armatureを含む前処理への対応。
- lilToon材質の変換、テクスチャの合成、影ランプ・輪郭・リムライトへの対応。
- VRChat SDK・Modular Avatar・NDMF・lilToonへの必須コンパイル依存を持たない構成。
- 警告レポート、変換ログ、キャンセル、出力ファイル表示。
- Windows向け「成果物をクリップボードにコピー」ボタン。最新の出力をコピーし、Resoniteで `Ctrl+V` または `Paste content from clipboard` により貼り付け。
- Windows x64用の変換ランタイムを同梱した `.unitypackage` 配布。

### 検証

- 任意パッケージを導入した環境、および未導入の独立環境でUnity EditModeテストを実行。
- アバター・固定アイテム・保存制限の切り替えを、Resoniteエンジンでの書き出しと読み戻しで確認。
- 詳細は[検証記録](VERIFICATION.md)を参照。

### 既知の制限

- 揺れ方と材質の見た目は近似変換。元環境との完全一致は保証しない。
- Expression Menu・Animatorの操作ロジック・Contacts・独自スクリプトなどは自動移植しない。
- Resonite本体のローカルインストールが必要。内部APIの更新により再ビルドが必要になる場合がある。
- 対応する設定の詳細は[日本語マニュアル](Assets/BEPFairyTech/ResoConverter/README-ja.md)を参照。
