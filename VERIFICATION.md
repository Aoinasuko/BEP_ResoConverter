# 検証記録

2026-09-11 / Windows / Unity 2022.3.22f1

## v0.2.0の表情機能

- VRChat SDK / Modular Avatar / NDMF / lilToon未導入の独立Unityプロジェクトで、EditModeテスト38件中33件成功、失敗0件、任意パッケージ専用5件スキップ。
- 上記パッケージを導入した別の独立Unityプロジェクトで38件すべて成功。VRC SDK 3.10.5 / Modular Avatar 1.18.7 / NDMF 1.14.8 / lilToon 2.3.4を使用。
- 開発中のシーンを保存・破棄せず、テストは専用プロジェクトで実行。開発用UnityではMCPでコンパイルを確認。
- 表情テストは、設定のシリアライズ、指定時刻のサンプリング、元の値を変えないこと、疎なクリップ間の値残り、無効な対象・時刻・名前の拒否、非対応カーブの警告を確認。
- MAで移動したRendererを参照で追跡し、正しい出力IDと処理後の初期値へ対応付けることを確認。削除・置換・EditorOnlyによる除外を別の同名対象へ誤接続しないことも検証。
- Resoniteエンジンで最終出力パッケージを読み戻し、表情の81検証項目に成功。左右8種類ずつの64組をテスト用の指ボーン入力として与え、分類結果、行の優先順、選択された表情値を確認。
- メニュー優先、手を動かしても固定が続くこと、初期状態への復帰、現在のハンドサインへの復帰、未指定Shapeが自動値へ戻ることを検証。
- 指入力には標準のContinuous Relayを介して継続更新を明示。試験用の固定参照へ入力を差し替えた場合にも、64組を連続して判定できることを確認。
- 未装着、指ボーンの欠落、装着ユーザーの入力がなくなった状態で、以前のハンドサインが残らず初期値・自動値へ戻ることを確認。Any条件が未装着状態で発動しないことも検証。
- 標準の瞬き `ValueDriver<float>` にテスト用の入力値 `0`、`0.85`、`0.2` を与え、既存ドライバーからLive Baseの中間フィールドを経由して元のBlendShapeへ届くことを確認。単に接続の存在を見る検証ではなく、3回の値の変化を確認。
- 上記の標準コンポーネント検証では、Headless・ユーザー1人のワールドで通常更新が休止するため、隔離した検証中だけ `ForceFullUpdateCycle` を有効化し、終了時に元へ戻す。変換後のアバターへこの設定は追加しない。
- AvatarManagerを持つ隔離した装着ユーザーを用意し、エンジンの `AvatarObjectSlot.Equip` / `Dequip` を実行。装着時のユーザー参照割当、実際の `UserFingerPoseSource` 経路から左右の指データが届くこと、表情メニューがユーザーへ登録されることを確認。
- 同じ検証で、2体目のアバターのメニュー選択が1体目の値に影響しないこと、装着解除時にユーザー参照と表情メニューの登録が解除されることを確認。
- 古い変換エンジンと新しいUnityコードの組み合わせでは、表情設定を黙って無視せず、対応エンジンのインポートを求める検査を追加。

- 最終版の変換エンジンを開発用Unityへ組み込み、MCPでコンパイル成功・エラー0件と表情対応フラグを確認。旧エンジンでは対応フラグが無効になることも確認。
- MCPから公開API `ResoConverter.ExportAsync` を呼び、シーン上のHumanoidアバターにハンドサイン2行・日本語メニュー2項目・選択した瞬きを設定して新規変換。生成したパッケージを最終版エンジンで読み戻し、上記81項目がすべて成功。2メッシュ・14,995頂点、標準身長1.8m、DynamicBone 7本、選択した瞬きと直接Visemeドライバーの保持も確認。
- Unity MCPから配布用unitypackageを生成。収録ファイルをリポジトリの全配布ファイルと照合し、開発用Unityへのインストール内容とも一致することを確認。バックエンドの表情対応フラグ、自己完結ランタイム、Resonite本体DLLの非同梱を検査。

上記はUnityのテスト・公開APIからの実データ変換と、Resoniteエンジンでのパッケージ読み戻し・テスト入力による検証です。

手の分類はResoniteの指データの曲げ角度を使った近似です。VRChatのジェスチャ番号を直接転送するものではありません。VR機器での実際のハンドサイン、メニュー操作、装着感や会話中の表情の見た目は未確認です。

## v0.1.2の文字列コピー

- 出力パッケージの絶対ファイルパスを `CF_UNICODETEXT` のみでコピーする方式へ変更。ファイル形式 `CF_HDROP` は登録しない。
- Unity MCPから更新後の公開API `ResoniteClipboard.CopyPackage` を実行。バージョン0.1.2と、元シーンが未保存状態のままであることを確認。
- 別のSTAプロセスでWPFクリップボードを読み戻し、Unicode文字列あり・ファイル一覧なし・画像なし・絶対パス完全一致・参照先ファイルの存在を確認。
- 不正な入力、存在しないファイル、相対パスの絶対パス化、日本語・空白・絵文字を含むUTF-16文字列と終端NULをテスト。自動テストはシステムのクリップボードを変更しない。
- VRChat SDK / Modular Avatar / NDMF / lilToon未導入の独立UnityプロジェクトでEditModeテスト25件成功、失敗0件、任意パッケージ専用4件スキップ。導入済みの開発プロジェクトではMCPでコンパイル成功・エラー0件を確認。
- 一時的なResoniteエンジン環境で、文字列だけを提供するClipboardInterfaceから公式の `ClipboardImporter.OnInputUpdate` を実行。`GetText` が1回呼ばれ、`PackageImporter` へ到達して2メッシュ・11,540頂点を読み込むことを確認。文字列表示へのフォールバックは0件。
- 通常の絶対パスと、日本語・空白を含む絶対パスの両方で上記の取り込みに成功。仮想キーの入力元検査やセキュリティ条件は変更していない。
- 標準Dashのボタンを実際に押す操作は未確認。文字列コピーとエンジンの取り込み経路の検証を、Dash UI全体の動作確認とは区別する。
- パッケージ形式と変換エンジンはv0.1.1から変更していない。v0.1.1の出力ファイルは新方式でコピーし直せる。

## v0.1のUnity Editor検証

- 開発用UnityプロジェクトのUnity MCP 4.2.0へ認証付きStreamable HTTPで接続。
- メニュー起動、コンパイル、コンソール、シーン読取、テスト、実データ変換をMCP経由で実行。
- VRChat SDK / Modular Avatar / NDMF / lilToonあり: EditModeテスト29件成功、失敗0件。
- 上記4パッケージを含まない独立Unityプロジェクト: 25件成功、失敗0件、任意パッケージ専用4件スキップ。
- 独立プロジェクトで公開APIから標準材質のモデルを実際に書き出し、5,029 bytesの `.resonitepackage` とレポートの生成まで成功。
- Bone Proxy、Merge Armatureを実際のNDMF前処理経由で検証。元の階層・メッシュを変更しないことを確認。
- ポーズとBlendShapeを固定したメッシュ、スケール、未指定PhysBoneルート、半径カーブ、表情設定を検証。
- DLL依存を独立化したprotobufランタイムは、公式ランタイムとの独立比較91件が成功。

## v0.1.1のUnity Editor再検証

- VRChat SDK / Modular Avatar / NDMF / lilToonありの開発プロジェクトで、更新後のコンパイルを確認。
- 同プロジェクトには未保存のシーンがあり、テスト実行ツールの保護により、この更新ではEditModeテストを再実行していない。v0.1時点の29件成功という記録は上記のとおり。
- 上記4パッケージを含まない独立Unityプロジェクトでは、更新版で25件成功、失敗0件、任意パッケージ専用4件スキップ。
- 独立プロジェクトの公開APIによる実際のモデル変換を実行し、生成パッケージの読み戻しと、後述するResoniteエンジン上のGrab処理を確認。

## v0.1〜v0.1.1のWindowsクリップボード検証と訂正

- 追加テスト4件で、無効な入力・存在しない出力の拒否、および日本語・空白・絵文字を含むファイル名をWindows Shellが正しく解釈できることを確認。自動テストは既存クリップボードを変更しない。
- Unity MCP経由で実際の出力パッケージをコピーし、別プロセスのWPF `Clipboard.GetFileDropList()` と `Clipboard.GetText()` で読み戻し、ファイル形式・絶対パス・ファイルの存在が一致することを確認。
- ファイル形式（CF_HDROP）とテキスト形式（CF_UNICODETEXT）の両方を提供。ResoniteのWindows実装にファイル一覧を読み、パッケージインポーターへ渡す経路があることを確認。ただし、この確認だけではDashの貼り付けボタンから実行される操作まで保証できない。
- コピー元ファイルを参照するため、貼り付け完了まで出力ファイルの移動・削除は不可。
- v0.1公開前の検証はWindows標準APIによる受け渡しと、別途のネイティブパッケージ読み戻しまでで、実行中のResoniteセッションでDashの貼り付けボタンを使う試験は未実施だった。
- 公開後、Resonite標準Dashの `Paste content from clipboard` ボタンだけが反応しないという実機報告を受けた。このUI操作の問題はv0.1.1でも未解消であり、従来のボタンからも取り込めるという案内を訂正。
- 同じ環境で、Windows Explorerからコピーした別のファイルも同ボタンでは取り込めないことをユーザーが確認。ツール固有のコピー形式より、Resonite側の入力・フォーカス状態に関係する可能性が高いが、直接の原因は未確定。
- v0.1.1時点の取り込み案内はキーボードの `Ctrl+V`、ファイルのドラッグ＆ドロップ、またはVR時のDashのFilesタブ。標準Dashのボタンに関する原因の最終確定や修正完了は主張していない。v0.1.2の変更と検証は冒頭に記載。

## v0.1のResoniteネイティブ変換・読み戻し

開発用シーンに配置したテストアバターを使用。モデルデータはリポジトリ・配布パッケージに含めません。

- アバター: 15メッシュ、38,931頂点。標準化後の実形状の高さ1.8m。
- 保存制限ON: SimpleAvatarProtectionが1件、インポート時の所有ユーザー再割当が有効。
- 瞬き: 指定したメッシュ・BlendShapeへの接続を確認。
- 揺れ物: DynamicBoneChainが12件、元の物理設定を保持する参照情報12件。
- ポーズ固定アイテム: アバター用ルート0件、動的ボーン0件、GrabbableとBoxCollider各1件。
- 保存制限OFF: SimpleAvatarProtectionが0件。
- 合成した表情設定: Visemeの明示的割当、Jawドライバー、瞬きOFF時に自動瞬きも残らないことを確認。

上記の初回検証では、GrabbableとBoxColliderの件数を確認しただけで、コライダーの各軸のサイズや実際のGrab処理を検証していなかった。両コンポーネントが存在していても掴めない不具合を見逃していたため、v0.1.1で検証範囲を追加した。

## v0.1.1のアイテムを掴む処理の回帰検証

問題が報告された2メッシュ・11,540頂点のパッケージを再現入力として、ローカルのResoniteエンジンで修正前後を比較。

| 確認項目 | 修正前 | 修正後 |
| --- | --- | --- |
| コライダーの範囲 | サイズが `(0.001, 0, 0)` で、見えるメッシュを覆わない | 読み込み済みメッシュの頂点範囲を覆う |
| 前後・左右・上下からのレイ | 6方向とも当たらない | 6方向とも当たる |
| レイで選択した対象のGrab処理 | 失敗 | 成功し、保持用スロットへの移動を確認 |
| 手の接触範囲からのGrab処理 | 接触なし・失敗 | 接触を検出し、保持用スロットへの移動を確認 |

- 修正後のパッケージは保存制限ON/OFFの両方で同じGrab検証に成功。
- 厚みのない三角形メッシュも検証し、コライダーの厚みが0にならず、6方向のレイ判定と両方のGrab処理が成功。
- 検証にはResoniteエンジンのPhysics・Grabberを使用。VR機器を装着した人による操作試験とは区別する。

パッケージの書き出し・読み戻し・上記のエンジン上の操作を確認しています。VR機器での装着感、会話中の見た目、他ユーザーによる保存操作、物理挙動の見た目が元モデルとどこまで一致するかは実利用での確認が必要です。標準Dashの貼り付けボタンの問題は未解消です。

詳細な実行ログと検証用ファイルは開発環境のみに保管しており、モデルデータや環境情報を含むためリポジトリには含めません。
